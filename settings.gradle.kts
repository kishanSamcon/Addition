pluginManagement {
    repositories {
        gradlePluginPortal()
        google() // ✅ REQUIRED for Android plugins like 'com.android.library'
        mavenCentral()
    }
}

rootProject.name = "Addition"
 